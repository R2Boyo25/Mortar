name: run-tests
on:
  push:
    branches:
      - master
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: kurocha/setup-cpp@v1
      - run: sudo apt install libboost-all-dev
      - run: python3 .avalon/comp.py
      - run: ./mortar --dt-success --dt-exit
